@model List<ProductItem>
@{
    ViewData["Title"] = "Product Item List";
}

<div class="container-fluid px-4 mt-4">
    <div class="card shadow-sm">
        <div class="card-header bg-gradient-primary d-flex justify-content-between align-items-center">
            <h2 class="mb-0 text-white">
                <i class="fas fa-layer-group me-2"></i>Product Item List
            </h2>
            <a asp-controller="ProductItem" asp-action="Upsert" class="btn btn-light">
                <i class="fas fa-plus-circle me-1"></i>Create New Product Item
            </a>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-borderless table-hover mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th class="ps-4">SKU</th>
                            <th>Product Name</th>
                            <th>Quantity In Stock</th>
                            <th class="text-end pe-4">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var productItem in Model)
                        {
                            <tr class="border-bottom">
                                <td class="ps-4 fw-bold">@productItem.SKU</td>
                                <td>@productItem.Product.Name</td>
                                <td>@productItem.QuantityInStock</td>
                                <td class="text-end pe-4">
                                    <div class="btn-group" role="group">
                                        <a asp-controller="ProductItem" asp-action="Upsert" asp-route-id="@productItem.Id"
                                           class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-edit me-1"></i>Edit
                                        </a>
                                        <a asp-controller="ProductItem" asp-action="Details" asp-route-id="@productItem.Id"
                                           class="btn btn-sm btn-outline-dark">
                                            <i class="fas fa-eye me-1"></i>Details
                                        </a>
                                        <a asp-controller="ProductItem" asp-action="Delete" asp-route-id="@productItem.Id"
                                           class="btn btn-sm btn-outline-danger">
                                            <i class="fas fa-trash-alt me-1"></i>Delete
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                @if (!Model.Any())
                {
                    <div class="text-center py-4 text-muted">
                        <i class="fas fa-box-open fa-3x mb-3"></i>
                        <p>No product items found. Create your first product item!</p>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<style>
    .bg-gradient-primary {
        background: linear-gradient(to right, #4e73de, #224abe);
    }

    .table-hover tbody tr:hover {
        background-color: rgba(0,0,0,0.05);
        transition: background-color 0.3s ease;
    }

    .btn-group .btn {
        display: inline-flex;
        align-items: center;
        transition: all 0.3s ease;
    }

        .btn-group .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
</style>

<!-- Optional: Font Awesome for icons -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
